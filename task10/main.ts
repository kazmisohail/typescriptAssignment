var names = ["Ali", "Abbas", "Ahmed"]; // array to store multiple data
let i = 0;
let s = names.length; // length function to find the size of an array

for (i = 0; i < s; i++) {
    console.log("Hi " + names[i] + ", Welcome to our company.");
}